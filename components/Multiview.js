import Video from './Video'

const Multiview = ({ data }) => {
    if (links === undefined)
        return null;

    return (
        data.Streams.map((streamInfo) => {
            
        })
    )
}

export default Multiview